
  # POS M.M. Farms UI Prototype

  This is a code bundle for POS M.M. Farms UI Prototype. The original project is available at https://www.figma.com/design/9yEqZYAwQEKDADssDNWkR0/POS-M.M.-Farms-UI-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  